{
  "name": "ecn-bot",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "lerna clean",
    "boot": "lerna bootstrap",
    "ogapi": "lerna run --scope=ecn-ogapi start --stream",
    "discord-bot": "lerna run --scope=ecn-discord-bot start --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "yup": "^0.32.11"
  }
}
